# Explain a fortune

Command line tool that explains a fortune, and "of course it's a toy".

Gets a fortune with fortune and gives it to a local LLM for analysis. Shows either fortune, explanation or both depending on command line arguments.

# Compile

mvn clean package
